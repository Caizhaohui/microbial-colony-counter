# 开发计划：批次标定学习（手机数录入 / 电脑点选+数录入，保留全部旧功能）

## 1. 背景与目标

### 1.1 用户痛点
- 自动计数对拍照条件敏感；菌落密集时准确率下降。
- 一次实验常需对 **十几个平板** 计数；同批次条件相似，适合「标定一张 → 批量应用」。

### 1.2 产品目标（本计划范围，已按需求修订）

| 端 | 新能力 | 说明 |
|----|--------|------|
| **手机 Web** | **图片 + 菌落数** | 上传/拍摄参考平板图，**手动输入**该盘人工计数 \(N\)；用于本机记录，或提交后端做批次参数学习（若连同一服务） |
| **电脑端**（桌面 GUI + PC 浏览器） | **批次标定 + 三种真值采集** | 见下表；完成后对剩余平板批量自动计数 |
| **全部端** | **旧功能完整保留** | 现有自动计数、参数调节、ROI、培养皿检测、分水岭、圆度、历史、保存等 **只增不改语义** |

### 1.3 电脑端：真值采集与批次学习（方式 3）

电脑端在「批次标定」流程中，参考盘真值支持 **三种策略**（用户任选）：

| 策略 | 用户操作 | 软件用法 |
|------|----------|----------|
| **策略 1：部分点选 + 学习** | 用鼠标在照片上点选 **部分** 菌落（建议 ≥10～30） | 用点估计菌落尺度/圆度先验；**必须同时输入本盘总真值 \(N\)**；搜索参数 θ* 使自动计数贴合 \(N\)，且符合点样本分布 |
| **策略 2：全量点选 + 学习** | 用鼠标点完照片上 **所有** 菌落 | 真值 \(N =\) 点数；点坐标用于匹配约束，降低「数对圈错」；输出 θ* 后批量计数 |
| **策略 3：图片 + 菌落数** | **不点选**，只上传参考图并输入人工总数 \(N\)**（与手机 Web 同形态） | 仅用 \(N\) 做参数搜索拟合；实现快、操作少，但无位置约束，假准风险高于策略 1/2 |

> 说明：策略 3 在电脑端与手机 Web 对齐，方便「人在手机上数完只填数」或「电脑上懒得点」两种习惯。

### 1.4 手机 Web 定位（修订后）
- **不是** 手机上逐一点菌落（触控密点成本高，本阶段不做手机点选为主路径）。
- **是**：参考盘 **图片 + 输入菌落数 \(N\)**。
- 典型用法：
  1. 实验室用手机拍参考盘 → 人工数完 → 填 \(N\) → 保存/上传；
  2. 若电脑已开同局域网 Web 服务，可将「图 + \(N\)」作为批次参考提交标定；
  3. 手机仍可继续使用 **旧自动计数** 处理其余盘（或回电脑做批量）。

### 1.5 非目标（本阶段不做）
- 不上深度学习/云端训练（后续可选）。
- 不删除、不替换现有单图自动计数 API 与 UI 入口。
- 手机端不做复杂「全屏点选计数」作为主功能（若未来要加，另开阶段）。

---

## 2. 原则：兼容性与「只增不改」

1. **算法层**：`process_image` 保持现有签名与行为；新逻辑以 **新模块** 调用它。
2. **API 层**：保留 `POST /api/v1/count`；新增标定/批次/标注相关接口。
3. **Web 层**：现有「上传 → 调参 → 开始计数」完整保留；通过 **模式/入口** 增加能力，不拆掉旧按钮语义。
4. **桌面层**：工具栏 **增加** 入口，不移除「打开/选区/处理/保存/参数」。
5. **数据**：批次配置、点集可导出 JSON；历史记录 **扩展字段**，兼容旧项。

---

## 3. 总体架构

```
真值采集（三选一，电脑；手机仅「图+N」）
  ├─ 策略1: 部分点 + N
  ├─ 策略2: 全量点 (N=|points|)
  └─ 策略3: 仅图 + N
              │
              ▼
     ┌────────────────────┐
     │  calibrator.py     │──── 调用现有 process_image(θ)
     │  搜索最优参数 θ*   │
     └─────────┬──────────┘
               ▼
     ┌────────────────────┐
     │  batch_runner      │  其余平板 × θ* → 结果表/CSV
     └────────────────────┘

入口映射：
  手机 Web  → 旧自动计数 | 新增「参考录入：图+N」（可触发标定或仅存档）
  电脑 Web  → 旧自动 | 批次标定（策略1/2/3）| 可选手动点选工具
  桌面 GUI  → 旧自动 | 批次工作台（策略1/2/3，鼠标点选）
```

---

## 4. 功能规格

### 4.1 手机 Web：图片 + 菌落数

**流程**
1. 进入「参考录入 / 批次学习」入口（与「自动计数」并列，不替换）。
2. 拍照或选图。
3. 输入框填写 **人工菌落数 \(N\)**（正整数校验）。
4. 可选备注（平板编号、实验名）。
5. 操作：
   - **保存到本机历史**（localStorage，字段含 `mode: ref_count_only`）；
   - 若服务可用：**提交标定** `POST /api/v1/calibrate`（strategy=`count_only`）→ 返回 θ* 摘要与参考盘预览；
   - 提示：完整批量更建议在电脑上继续（多图选择体验更好）。

**交互要点**
- 大号数字键盘友好输入；防止误提交空图/空 \(N\)。
- 标定进行中显示进度/耗时提示。
- **不提供** 手机点选菌落作为主路径（避免与「图+N」定位冲突）。

**验收**
- 仅图+N 可保存历史；在线时可完成 count_only 标定并看到预览计数与 \(N\) 对比。
- 旧「开始计数」自动流程零回归。

---

### 4.2 电脑端：批次标定（策略 1 / 2 / 3）

**整体流程**
1. **新建批次**（名称、备注、日期可选）。
2. **加载参考盘图片**。
3. **选择真值策略**：
   - 策略 1：部分点选 + 输入 \(N\)
   - 策略 2：全量点选（自动 \(N=\) 点数）
   - 策略 3：仅输入 \(N\)（同手机）
4. **开始学习/标定** → 展示：θ* 摘要、参考盘自动结果 vs \(N\)、标注/二值预览、拟合误差与置信提示。
5. **用户确认**（可微调参数后「重新标定」或「锁定 θ*」）。
6. **批量**：多选其余平板 → 用 θ* 自动计数 → 结果表。
7. **导出**：CSV/JSON、可选标注图；批次配置存盘以便复用。

#### 策略 1：部分点选 + 学习
- 鼠标左键加点、右键/橡皮删点、撤销；支持滚轮缩放与拖拽平移。
- 样本点 \(k \ge k_{min}\)（默认 15，可配置）；不足时禁止标定并提示。
- **必须** 输入整盘真值 \(N\)。
- 标定：用点分布收窄 `min_area`/`max_area`（及可选圆度）搜索范围，目标 \(\min |count(I,θ)-N|\)。

#### 策略 2：全量点选 + 学习
- 用户点完所有可见菌落；界面实时显示点数作为 \(N\)。
- 标定：拟合 \(N\) + **检测中心与人工点匹配分**（距离阈值内匹配比例），抑制假准。
- 可选：先跑自动 → 转为可编辑点 → 用户增删后作为全量真值（增强，不强制）。

#### 策略 3：图片 + 菌落数
- 与手机一致：只填 \(N\)，无点集。
- 纯参数搜索贴合 \(N\)；UI 提示「无位置信息，建议同批次拍照条件一致；密菌落优先策略 1/2」。

**验收**
- 三种策略均可产出 θ* 并完成批量。
- 策略 2 在有点匹配时优于「乱圈但数对」的退化解（可用合成/真实样例抽检）。
- 旧单图自动流程零回归。

---

### 4.3 电脑端辅助：独立「手动点选计数」工具（可选但建议做）
- 不仅嵌在批次里：也可单图「只点数、不学习」——方便用户纯手点一盘出报告。
- 与策略 1/2 **共用** 点选组件；不删除旧自动。

---

## 5. 模块与文件规划

### 5.1 后端（新增为主）

| 路径 | 职责 |
|------|------|
| `backend/core/pointset.py` | 点集结构、显示坐标↔原图像素、标注图绘制 |
| `backend/core/calibrator.py` | 三策略统一标定入口；参数搜索；全量点匹配评分；部分点先验 |
| `backend/core/batch.py` | 多图应用 θ*、汇总 |
| `backend/schemas.py` | **追加** 模型，不删旧字段 |
| `backend/main.py` | **追加** 路由；`/api/v1/count` 行为不变 |
| `backend/test_calibrator.py` / `test_pointset.py` | 单测 |

**建议 API（新增）**

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/v1/calibrate` | 参考图 + strategy + 可选 points + 可选/必填 N → θ* + 预览 |
| POST | `/api/v1/batch_count` | 多图 + θ* → 结果列表 |
| POST | `/api/v1/annotate/render` | 原图 + 点集 → 标注图 base64 |

`strategy` 枚举建议：
- `partial_points` — 策略 1  
- `full_points` — 策略 2  
- `count_only` — 策略 3（手机与电脑共用）

**标定搜索（初版）**  
`blur_ksize`、阈值相关、`min_area`/`max_area`、`min_circularity`、`use_watershed`、`detect_petri_dish`、`min_distance_from_edge` 等。  
目标：单张参考图 CPU **≤ 30s**。

### 5.2 Web 前端（`backend/static/index.html` 等）

| 模式 | 内容 | 设备 |
|------|------|------|
| `auto` | **现有**自动计数（完整保留） | 全端 |
| `ref_entry` | 图 + 输入 \(N\)；可保存/提交 count_only 标定 | **手机默认展示此新入口之一**；电脑也有 |
| `batch` | 批次工作台：策略 1/2/3 + 批量 | **电脑宽屏主推**；手机可简化为仅 ref_entry 或提示去电脑批量 |
| `manual_points` | 点选组件（供策略 1/2 与可选单图手点） | **电脑** |

设备策略：
```text
窄屏：显著「自动计数」+「参考录入(图+N)」；批次批量入口弱化或引导电脑
宽屏：自动 | 参考录入(图+N) | 批次标定(含点选策略) | 可选手动点选
```

### 5.3 桌面 GUI（`main.py`）

**增加**（不删旧控件）：
1. 「📦 批次标定」→ 工作台（策略 1/2/3，进程内调 calibrator，可不走 HTTP）。
2. 点选工具：缩放、左键加点、右键删点、计数显示（策略 1/2）。
3. 策略 3：参考图 + 输入 \(N\) 表单。
4. 批量选文件、结果表、导出 CSV、保存 θ* JSON。

---

## 6. 数据模型（核心）

```text
ColonyPoint: { id, x, y, source: "manual"|"auto" }  # 原图像素

CalibrateRequest:
  strategy: partial_points | full_points | count_only
  total_gt: Optional[int]   # count_only 与 partial_points 必填；full_points 可由点数推导
  points: Optional[List[ColonyPoint]]
  # 可选：用户预设的硬约束（强制开培养皿检测等）

CalibratedParams:  # 与 process_image kwargs 对齐 + 元数据
  ...process kwargs...
  strategy, ref_count_gt, fit_error, match_score?, version

BatchJob: { id, name, params, items[] }
```

---

## 7. 分阶段实施计划

### 阶段 0：约定与基线（0.5 天）
- [ ] 兼容性清单 + 自动计数回归基线（`test1.jpg` / `test2.jpg`）。
- [ ] strategy 枚举与 JSON 导出格式定稿。
- [ ] 准备一批真实图（≥1 张有人工 \(N\)）。

### 阶段 1：点集与绘制基础（1–2 天）
- [ ] `pointset.py` + 单测（坐标往返、绘制）。
- [ ] 不改 `process_image` 默认行为。

### 阶段 2：标定器三策略（3–5 天）
- [ ] `count_only`（策略 3）参数搜索。
- [ ] `full_points`（策略 2）+ 点匹配分。
- [ ] `partial_points`（策略 1）点先验收窄搜索 + \(N\) 拟合。
- [ ] 脚本/单测：拟合误差与超时可控。

### 阶段 3：API（1–2 天）
- [ ] `POST /api/v1/calibrate`、`batch_count`、`annotate/render`。
- [ ] OpenAPI/字段文档；`/api/v1/count` 回归。

### 阶段 4：手机 Web「图 + N」（2 天）
- [ ] 新入口 UI：上传、输入 \(N\)、保存历史、提交标定、预览对比。
- [ ] 旧自动计数 UI/流程不动。

### 阶段 5：电脑 Web 批次工作台（3–5 天）
- [ ] 策略切换 UI；点选画布（缩放/加点/删点）。
- [ ] 标定预览 → 锁定 θ* → 多图批量 → CSV。
- [ ] 策略 3 与手机同一 API。

### 阶段 6：桌面 GUI 批次工作台（3–4 天）
- [ ] 与 Web 逻辑对齐（优先进程内调用 core，避免重复）。
- [ ] 鼠标点选体验（策略 1/2）+ 图+N（策略 3）。
- [ ] 批量与导出。

### 阶段 7：回归与文档（2 天）
- [ ] 全路径回归旧功能。
- [ ] README / 使用说明追加章节；版本如 **v1.2.0**。

**合计预估**：约 **2.5～4 周**（单人）。

---

## 8. 里程碑与验收

| 里程碑 | 交付 | 验收 |
|--------|------|------|
| **M1** | pointset + calibrator 三策略 | 单测通过；参考图可输出 θ* |
| **M2** | 标定/批量 API | count 旧接口不变；三 strategy 可用 |
| **M3** | 手机 Web 图+N | 保存与 count_only 标定可用 |
| **M4** | 电脑 Web 批次 | 策略 1/2/3 + 批量 + CSV |
| **M5** | 桌面批次 | 鼠标点选 + 图+N + 批量 |
| **M6** | 回归与文档 | 旧功能无破坏 |

**建议数值门槛（可按真实数据调整）**
- 参考盘：\(|pred - N| / N \le 5\%\) 或绝对误差 ≤ 3～5。
- 同批其余盘：相对默认参数有提升，或误差中位数 ≤ 10%～15%。
- 文案强调：**按批次学习**，换条件需重标定。

---

## 9. UI 信息架构（示意）

### 9.1 手机 Web
```
[自动计数(旧)] [参考录入·图+N]
────────────────────────────
[ 选图/拍照 ]
[ 人工菌落数: ____ ]
[ 保存 ] [ 提交学习并预览 ]
（批量多图：提示使用电脑）
```

### 9.2 电脑 Web / 桌面
```
[自动计数(旧)] [批次标定] [可选: 仅手动点选]
批次步骤：
  参考图 → 选策略
    (1) 部分点选 + 输入N
    (2) 全量点选
    (3) 仅输入N
  → 开始学习 → 确认θ* → 批量选图 → 结果/导出
```

---

## 10. 风险与对策

| 风险 | 对策 |
|------|------|
| 仅 \(N\) 拟合假准 | 文案引导密菌落用策略 1/2；匹配分/参数范围约束 |
| 标定慢 | 两阶段搜索、限迭代、缓存预处理 |
| 手机批量体验差 | 手机主做图+N；批量主路径放电脑 |
| 点选与 ROI 冲突 | 批次/点选模式与 ROI 互斥 |
| 回归破坏旧功能 | 每阶段跑算法脚本 + 手测旧自动流 |
| 用户以为一次标定终身通用 | 产品文案：**本批次有效** |

---

## 11. 测试计划

1. 单元：点集、三策略标定、匹配。  
2. 回归：`/api/v1/count` 与桌面/Web 旧自动路径。  
3. E2E 手机：图+N 保存；在线 count_only 预览。  
4. E2E 电脑：策略 1 / 2 / 3 各跑通一批量。  
5. 兼容：旧书签打开 Web，默认可自动计数。

---

## 12. 文档与发布

- `README.md`、`使用说明.txt`：**追加** 手机参考录入、电脑三策略批次；保留原说明。
- 版本标签如 `v1.2.0`；打包入口 `main.py` / `web_launcher.py` 不变。

---

## 13. 任务拆分顺序

```text
T1  pointset + 单测
T2  calibrator: count_only（策略3）
T3  calibrator: full_points（策略2）
T4  calibrator: partial_points（策略1）
T5  API calibrate / batch_count / annotate/render
T6  手机 Web：图+N 入口
T7  电脑 Web：点选组件 + 批次 UI
T8  桌面 GUI：批次工作台
T9  导出/配置存读 + 回归 + 文档
```

**依赖**：T2 完成后即可并行 T6（手机）；T3/T4 完成后接 T7/T8。

---

## 14. 成功定义（一句话）

> 手机可「拍照 + 填菌落数」参与参考标定；电脑可用「部分点选 / 全量点选 / 仅填数」三种方式学到本批次参数并批量计数；原有自动计数与全部旧功能保持可用。
